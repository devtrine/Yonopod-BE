info:
  name: Get File with Tags
  type: http
  seq: 5

http:
  method: GET
  url: "{{host}}/api/v1/tags/8/files"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
