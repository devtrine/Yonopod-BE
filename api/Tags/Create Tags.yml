info:
  name: Create Tags
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/v1/"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This request retrieves a list of users from the JSONPlaceholder API.
