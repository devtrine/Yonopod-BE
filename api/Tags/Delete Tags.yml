info:
  name: Delete Tags
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{host}}/api/v1/tags/7"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
