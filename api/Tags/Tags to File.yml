info:
  name: Tags to File
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/api/v1/tags/7/files/3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
