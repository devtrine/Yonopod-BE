info:
  name: Delete Tags from Files
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{host}}/api/v1/tags/7/files/3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
