info:
  name: Search by Tag
  type: http
  seq: 6

http:
  method: GET
  url: "{{host}}/api/v1/search?q=photo&tag=ArsipBiru"
  params:
    - name: q
      value: photo
      type: query
    - name: tag
      value: ArsipBiru
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
