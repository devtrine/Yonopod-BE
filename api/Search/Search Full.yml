info:
  name: Search Full
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/v1/search?q=readme"
  params:
    - name: q
      value: readme
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
