info:
  name: Search Folder
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/api/v1/search?folderId=1"
  params:
    - name: folderId
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
