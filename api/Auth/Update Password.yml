info:
  name: Update Password
  type: http
  seq: 5

http:
  method: PUT
  url: "{{host}}/api/v1/auth/me/password"
  body:
    type: form-urlencoded
    data:
      - name: current_password
        value: admin123
      - name: new_password
        value: luthfi277
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
