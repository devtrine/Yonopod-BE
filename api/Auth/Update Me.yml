info:
  name: Update Me
  type: http
  seq: 4

http:
  method: PUT
  url: "{{host}}/api/v1/auth/me"
  body:
    type: form-urlencoded
    data:
      - name: full_name
        value: Luthfi Malik
      - name: avatar_url
        value: https://example.com
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
