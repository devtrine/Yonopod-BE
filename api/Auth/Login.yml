info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5000/api/v1/auth/login
  body:
    type: form-urlencoded
    data:
      - name: email
        value: admin@yono.dev
      - name: password
        value: admin123
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
