info:
  name: Download link
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:5000/api/v1/shares
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
