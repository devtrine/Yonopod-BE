info:
  name: Download
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:6767/api/v1/files/{{id}}/download
  auth: inherit

runtime:
  variables:
    - name: id
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
