info:
  name: Delete
  type: http
  seq: 6

http:
  method: DELETE
  url: http://localhost:6767/api/v1/files/{{id}}
  auth: inherit

runtime:
  variables:
    - name: id
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
