info:
  name: List Largest
  type: http
  seq: 9

http:
  method: GET
  url: "{{host}}/api/v1/files/largest?limit=25"
  params:
    - name: limit
      value: "25"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
