info:
  name: Update
  type: http
  seq: 5

http:
  method: PUT
  url: http://localhost:6767/api/v1/files/{{id}}
  body:
    type: form-urlencoded
    data:
      - name: is_favorite
        value: "true"
      - name: name
        value: wfcwawsw
  auth: inherit

runtime:
  variables:
    - name: id
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
