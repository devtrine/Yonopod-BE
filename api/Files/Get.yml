info:
  name: Get
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/api/v1/files/{{id}}"
  auth: inherit

runtime:
  variables:
    - name: id
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
