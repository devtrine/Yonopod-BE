info:
  name: Record Access
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5000/api/v1/recent
  body:
    type: json
    data: |-
      {
        "file_id":3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
