info:
  name: Clear History
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{host}}/api/v1/recent"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
