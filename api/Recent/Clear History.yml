info:
  name: Clear History
  type: http
  seq: 3

http:
  method: DELETE
  url: http://localhost:5000/api/v1/recent
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
