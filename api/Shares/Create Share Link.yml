info:
  name: Create Share Link
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5000/api/v1/shares
  body:
    type: json
    data: |-
      {
        "folder_id": 1,
        "share_type": "link",
        "vault_password":"H177013",
        "password": "admin123",
        "download_limit": 2,
        "expires_at": "2026-12-31T23:59:59Z"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
