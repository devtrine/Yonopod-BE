info:
  name: Create Share Link
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5000/api/v1/shares
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
