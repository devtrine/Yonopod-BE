info:
  name: Update Link Shares
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:5000/api/v1/shares/5
  body:
    type: json
    data: |-
      {
        "download_limit": 5,
        "permission": "read_only"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
