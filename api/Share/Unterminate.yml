info:
  name: Unterminate
  type: http
  seq: 5

http:
  method: PATCH
  url: http://localhost:5000/api/v1/shares/{{id}}/unterminate
  auth: inherit

runtime:
  variables:
    - name: id
      value: e7b7378f-3530-43d0-a204-01fd6df992ca

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
