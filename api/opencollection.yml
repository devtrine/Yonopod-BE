opencollection: 1.0.0

info:
  name: api copy
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  variables:
    - name: host
      value: http://localhost:6767
      disabled: true
    - name: host
      value: https://yonopod.lowres.my.id
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
    presets:
      request:
        type: http
        url: http://localhost:6767/api/v1/
