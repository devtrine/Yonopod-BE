info:
  name: test
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:6767/api/v1/notifications
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
