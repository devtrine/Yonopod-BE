info:
  name: Get notification
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/v1/notifications"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
