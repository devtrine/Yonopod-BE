info:
  name: Get notification
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:5000/api/v1/notification
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
