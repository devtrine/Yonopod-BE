info:
  name: Read all
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/api/v1/notification/read-all"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
