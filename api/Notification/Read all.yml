info:
  name: Read all
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:5000/api/v1/notification/read-all
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
