info:
  name: create notification
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5000/api/v1/notifications
  body:
    type: json
    data: |-
      {
        "title": "Welcome to Huby!",
        "message": "Terima kasih sudah mendaftar di sistem kami.",
        "type": "info",
        "data": { "action_url": "/dashboard" }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
