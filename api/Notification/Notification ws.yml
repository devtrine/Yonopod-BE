info:
  name: Notification ws
  type: http
  seq: 5

http:
  method: POST
  url: {{host}}/api/v1/notifications/test
  body:
    type: json
    data: |-
      {
        "title": "Pesan Masuk!",
        "message": "Ini adalah pengujian WebSocket dari HTTP request.",
        "type": "info"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
