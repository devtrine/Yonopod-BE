info:
  name: Notification ws
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:5000/api/v1/notifications/test
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
