info:
  name: Unlock
  type: http
  seq: 10

http:
  method: POST
  url: http://localhost:5000/api/v1/folders/11/unlock
  body:
    type: json
    data: |
      {
        "vault_password":"jkl123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
