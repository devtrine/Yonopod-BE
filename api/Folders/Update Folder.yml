info:
  name: Update Folder
  type: http
  seq: 4

http:
  method: PUT
  url: "{{host}}/api/v1/folders/11"
  body:
    type: json
    data: |-
      {
        "name":"Reel"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
