info:
  name: Restore
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:5000/api/v1/folders/11/restore
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
