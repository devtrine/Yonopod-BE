info:
  name: Create Folder
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:5000/api/v1/folders
  body:
    type: json
    data: |-
      {
        "name":"Reels",
        "parent_id":null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
