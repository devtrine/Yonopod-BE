info:
  name: Permanent Delete
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{host}}/api/v1/folders/11/permanent"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
