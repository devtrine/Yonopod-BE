info:
  name: Unfavorites
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{host}}/api/v1/favorites/3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
