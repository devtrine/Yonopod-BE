info:
  name: List Favorites
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/api/v1/favorites"
  params:
    - name: type
      value: folder
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
