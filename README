# Setup Yono-BE

## Setup
1. Prepare postgres and make sure it is running.
2. cp .env.example .env
3. edit .env
4. npm install
5. npx sequelize-cli db:migrate
6. npx sequelize-cli db:seed:all // for seeding data(opt)

## Running the server
7. npm run dev
